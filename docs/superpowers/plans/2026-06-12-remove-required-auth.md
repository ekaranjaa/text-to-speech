# Text-to-Speech — Remove Required Auth + Repo Setup — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the Kokoro + OpenReader text-to-speech stack usable without a login (OpenReader anonymous sessions), move secrets/config into a git-ignored `.env`, add README/.env.example/.gitignore, and ship it as a public GitHub repo with an opening PR — mirroring the sibling `~/Code/speech-to-text`.

**Architecture:** Single hand-written `docker-compose.yml` (Kokoro-FastAPI on `8880`, OpenReader on `3003`/`8333`). Auth login requirement removed via `USE_ANONYMOUS_AUTH_SESSIONS=true`; `AUTH_SECRET`/`BASE_URL`/`ADMIN_EMAILS`/ports sourced from `.env`. Committed `.env.example` carries placeholders.

**Tech Stack:** Docker Compose v2, `ghcr.io/remsky/kokoro-fastapi-cpu`, `ghcr.io/richardr1126/openreader`.

**Spec:** `docs/superpowers/specs/2026-06-12-remove-required-auth-design.md`

**Working directory:** `~/Code/text-to-speech` (NOT yet a git repo — initialized in Task 5).

**Note on TDD:** This is infrastructure/config, not application code, so there are no unit tests. Verification is concrete: `docker compose config` for static validation, plus `git status`/grep checks that no secret is committed. Commands and expected output are given explicitly. Commits happen at the end (Task 5) because the repo does not exist until then; each earlier task is still verified independently.

---

### Task 1: Add `.env` (real values, git-ignored) and `.env.example` (placeholders)

`.env` must exist before `docker compose config` can resolve the compose file in Task 2, so it comes first.

**Files:**
- Create: `~/Code/text-to-speech/.env`
- Create: `~/Code/text-to-speech/.env.example`

- [ ] **Step 1: Write `.env`** with a freshly-generated secret (do NOT reuse the
  old committed secret — it must never appear in any tracked file). Generate the
  secret first, then write the file:

Run: `cd ~/Code/text-to-speech && SECRET=$(openssl rand -hex 32) && cat > .env <<EOF`
…then the heredoc body (substituting `$SECRET`):

```
# ── OpenReader + Kokoro text-to-speech configuration ────────────────────────
# Local config (git-ignored). Copy from .env.example for a fresh deployment.

# Host ports (change only if they collide with something else).
OPENREADER_PORT=3003
KOKORO_PORT=8880

# Public URL the browser uses to reach OpenReader — must match OPENREADER_PORT.
BASE_URL=http://localhost:3003

# Session-signing secret (mandatory in OpenReader v4+). Freshly generated by
# this step via `openssl rand -hex 32`. Changing it just re-issues sessions.
AUTH_SECRET=<freshly generated 64-hex-char value — substituted at write time>

# Admin user(s), comma-separated. The admin owns the shared TTS provider that
# anonymous users rely on. First user matching this list becomes admin.
ADMIN_EMAILS=__ADMIN_EMAIL__   # executor substitutes the real admin email here
```

Concretely, write it in one shot so the live secret only ever exists in the
git-ignored `.env`:

\`\`\`bash
cd ~/Code/text-to-speech
SECRET=$(openssl rand -hex 32)
cat > .env <<EOF
# ── OpenReader + Kokoro text-to-speech configuration ────────────────────────
# Local config (git-ignored). Copy from .env.example for a fresh deployment.

# Host ports (change only if they collide with something else).
OPENREADER_PORT=3003
KOKORO_PORT=8880

# Public URL the browser uses to reach OpenReader — must match OPENREADER_PORT.
BASE_URL=http://localhost:3003

# Session-signing secret (mandatory in OpenReader v4+). Freshly generated via
# openssl rand -hex 32. Changing it just re-issues sessions.
AUTH_SECRET=$SECRET

# Admin user(s), comma-separated. The admin owns the shared TTS provider that
# anonymous users rely on. First user matching this list becomes admin.
ADMIN_EMAILS=__ADMIN_EMAIL__   # executor substitutes the real admin email here
EOF
echo wrote-.env
\`\`\`

- [ ] **Step 2: Write `.env.example`** — same keys, safe placeholders:

```
# ── OpenReader + Kokoro text-to-speech configuration ────────────────────────
# Copy this file to `.env` (which is git-ignored) before `docker compose up`:
#     cp .env.example .env

# Host ports (change only if they collide with something else).
OPENREADER_PORT=3003
KOKORO_PORT=8880

# Public URL the browser uses to reach OpenReader — must match OPENREADER_PORT.
BASE_URL=http://localhost:3003

# Session-signing secret (mandatory in OpenReader v4+). Generate one with:
#     openssl rand -hex 32
AUTH_SECRET=

# Admin user(s), comma-separated. The admin owns the shared TTS provider that
# anonymous users rely on. The first user matching this list becomes admin.
ADMIN_EMAILS=you@example.com
```

- [ ] **Step 3: Verify** both files exist and differ only as intended:

Run: `cd ~/Code/text-to-speech && test -f .env && test -f .env.example && diff <(grep -o '^[A-Z_]*=' .env) <(grep -o '^[A-Z_]*=' .env.example) && echo KEYS_MATCH`
Expected: `KEYS_MATCH` (same variable keys in both files).

---

### Task 2: Edit `docker-compose.yml` — anonymous sessions + `.env` references

**Files:**
- Modify: `~/Code/text-to-speech/docker-compose.yml`

- [ ] **Step 1: Replace the file** with this content (Kokoro service unchanged; OpenReader ports and `environment` block updated):

```yaml
services:
  # Kokoro-FastAPI — open-source TTS engine, OpenAI-compatible API
  # Settings copied from the OpenReader Kokoro guide (CPU profile).
  kokoro-tts:
    image: ghcr.io/remsky/kokoro-fastapi-cpu:v0.2.4
    container_name: kokoro-tts
    ports:
      - "${KOKORO_PORT:-8880}:8880"
    environment:
      - ONNX_NUM_THREADS=8
      - ONNX_INTER_OP_THREADS=4
      - ONNX_EXECUTION_MODE=parallel
      - ONNX_OPTIMIZATION_LEVEL=all
      - ONNX_MEMORY_PATTERN=true
      - ONNX_ARENA_EXTEND_STRATEGY=kNextPowerOfTwo
      - API_LOG_LEVEL=DEBUG
    restart: unless-stopped

  # OpenReader — reader UI + TTS orchestrator
  # Mirrors the docs' "Localhost" docker run example, adapted for Compose.
  openreader:
    image: ghcr.io/richardr1126/openreader:latest
    container_name: openreader
    ports:
      - "${OPENREADER_PORT:-3003}:3003" # Web UI / API
      - "8333:8333" # Embedded SeaweedFS S3 (required by docs)
    environment:
      # --- TTS provider (first-boot seed into an admin "default-openai" provider) ---
      - API_BASE=http://kokoro-tts:8880/v1
      - API_KEY=none

      # --- Auth (no login required) ---
      # OpenReader v4+ cannot disable auth entirely (BASE_URL + AUTH_SECRET are
      # mandatory), but anonymous sessions let users reach /app without signing
      # in or creating an account. Docs: https://docs.openreader.richardr.dev/configure/auth
      - USE_ANONYMOUS_AUTH_SESSIONS=true
      # BASE_URL must match how users reach the app in their browser.
      - BASE_URL=${BASE_URL:-http://localhost:3003}
      - AUTH_SECRET=${AUTH_SECRET:?set AUTH_SECRET in .env (openssl rand -hex 32)}
      # Admin owns the shared TTS provider anonymous users rely on.
      - ADMIN_EMAILS=${ADMIN_EMAILS}
    volumes:
      # Persists SeaweedFS blob data, SQLite metadata, migrations, runtime temp state.
      - openreader_docstore:/app/docstore
    depends_on:
      - kokoro-tts
    restart: unless-stopped

volumes:
  openreader_docstore:
```

- [ ] **Step 2: Validate** the compose file resolves against `.env` and anonymous sessions is set:

Run: `cd ~/Code/text-to-speech && docker compose config | grep -E 'USE_ANONYMOUS_AUTH_SESSIONS|BASE_URL|AUTH_SECRET'`
Expected: rendered values show `USE_ANONYMOUS_AUTH_SESSIONS: "true"`, the `BASE_URL`, and the `AUTH_SECRET` from `.env` — and the command exits 0 (no "variable is not set" warnings causing empty required values).

---

### Task 3: Add `.gitignore`

**Files:**
- Create: `~/Code/text-to-speech/.gitignore`

- [ ] **Step 1: Write `.gitignore`:**

```
# Local config — keeps the real AUTH_SECRET and admin email out of git.
.env

# macOS
.DS_Store
```

- [ ] **Step 2: Verify** (after git init in Task 5 this is re-checked; for now just confirm content):

Run: `cd ~/Code/text-to-speech && grep -q '^.env$' .gitignore && grep -q '.DS_Store' .gitignore && echo GITIGNORE_OK`
Expected: `GITIGNORE_OK`

---

### Task 4: Write `README.md`

**Files:**
- Create: `~/Code/text-to-speech/README.md`

- [ ] **Step 1: Write `README.md`:**

```markdown
# text-to-speech

Self-hosted **text-to-speech**: paste or upload text and listen to it (or export
audio) from a browser reader UI. Runs entirely locally.

Powered by [**Kokoro-FastAPI**](https://github.com/remsky/Kokoro-FastAPI) (the
TTS engine, OpenAI-compatible API) and
[**OpenReader**](https://github.com/richardr1126/openreader) (the reader UI +
orchestrator). This is the text-to-speech sibling of the `speech-to-text`
(Whishper + faster-whisper) setup.

## Stack

Two containers (see `docker-compose.yml`):

| Service | Role | Host-exposed? |
|---|---|---|
| `openreader` | reader web UI + TTS orchestrator | yes, **http://localhost:3003** (+ `8333` SeaweedFS S3) |
| `kokoro-tts` | Kokoro-FastAPI TTS engine, OpenAI-compatible API | yes, **http://localhost:8880** |

CPU profile — no GPU required.

## No sign-in required

OpenReader runs in **anonymous-session** mode
(`USE_ANONYMOUS_AUTH_SESSIONS=true`), so you go straight to the reader without
creating an account or logging in. Note: OpenReader v4+ cannot turn auth off
entirely — `AUTH_SECRET` and `BASE_URL` are still required at startup — so this
removes the *login requirement*, not the auth subsystem. An admin
(`ADMIN_EMAILS`) is still configured because the admin owns the shared TTS
provider that anonymous sessions use. See
https://docs.openreader.richardr.dev/configure/auth.

## Quick start

\`\`\`bash
cd ~/Code/text-to-speech
cp .env.example .env      # create your local config
# then edit .env: set AUTH_SECRET (openssl rand -hex 32) and ADMIN_EMAILS
docker compose up -d      # pulls images, starts the stack
\`\`\`

Then open **http://localhost:3003** — no login needed.

\`\`\`bash
docker compose logs -f openreader   # follow app logs
docker compose ps                   # service status
docker compose down                 # stop (data is kept in the volume)
\`\`\`

## Configuration (`.env`)

| Variable | Meaning | Default |
|---|---|---|
| `OPENREADER_PORT` | host port for the reader UI | `3003` |
| `KOKORO_PORT` | host port for the Kokoro engine | `8880` |
| `BASE_URL` | public URL the browser uses — must match `OPENREADER_PORT` | `http://localhost:3003` |
| `AUTH_SECRET` | session-signing secret (mandatory). Generate: `openssl rand -hex 32` | — |
| `ADMIN_EMAILS` | admin user(s), comma-separated; owns the shared TTS provider | — |

To change the UI port, set `OPENREADER_PORT` **and** `BASE_URL` in `.env`, then
`docker compose up -d`.

## Data

OpenReader state (SeaweedFS blobs, SQLite metadata, migrations, temp state)
persists in the `openreader_docstore` Docker volume. Remove it
(`docker compose down -v`) for a clean slate.

## Notes & troubleshooting

- **Changed `AUTH_SECRET`?** Existing anonymous sessions are invalidated; the app
  issues fresh ones on next visit.
- **First boot pulls images** (Kokoro + OpenReader), so the first start is slow.
- **GPU:** not used — this stack is CPU-only by design.
```

> When writing the file, replace the `\`\`\`` escapes above with real triple-backtick fences.

- [ ] **Step 2: Verify** the README mentions the key facts:

Run: `cd ~/Code/text-to-speech && grep -q 'USE_ANONYMOUS_AUTH_SESSIONS' README.md && grep -q 'localhost:3003' README.md && echo README_OK`
Expected: `README_OK`

---

### Task 5: Initialize repo, commit, create public GitHub repo, open PR

**Files:** none (git operations only).

- [ ] **Step 1: Confirm no secret or personal email leaks into tracked files.**

The live `AUTH_SECRET` exists only in the git-ignored `.env`; the real admin
email is also only in `.env` (committed docs use the `__ADMIN_EMAIL__`
placeholder). Verify nothing sensitive sits outside `.env`:

Run: `cd ~/Code/text-to-speech && EMAIL=$(sed -n 's/^ADMIN_EMAILS=//p' .env) && SECRET=$(sed -n 's/^AUTH_SECRET=//p' .env) && grep -RIn --exclude-dir=.git --exclude=.env -e "$SECRET" -e "$EMAIL" . || echo CLEAN`
Expected: no matches printed (only `exit=1` from grep finding nothing). The
secret and the real email appear **only** in `.env`. If any committed file
(compose, README, `.env.example`, or `docs/`) matches, redact it before
committing.

NOTE: `.env` is excluded by `.gitignore` in the actual commit; confirm that next.

- [ ] **Step 2: Initialize git and stage, then confirm `.env` is ignored.**

Run:
\`\`\`bash
cd ~/Code/text-to-speech
git init -q
git add -A
git status --short
\`\`\`
Expected: `README.md`, `.env.example`, `.gitignore`, `docker-compose.yml`, and `docs/...` are staged (`A`). `.env` and `.DS_Store` are **absent** from the list (ignored).

- [ ] **Step 3: Baseline commit on `main`.**

\`\`\`bash
cd ~/Code/text-to-speech
git branch -M main
git commit -q -m "Initial commit: Kokoro + OpenReader text-to-speech stack

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
git log --oneline -1
\`\`\`
Expected: one commit printed.

- [ ] **Step 4: Create feature branch with the auth/docs work already in the baseline.**

Because everything was authored at once, split the history so the PR is meaningful: keep the bare compose as `main`, put the auth-removal + docs on the branch.

\`\`\`bash
cd ~/Code/text-to-speech
git checkout -b feat/no-auth-and-docs
git log --oneline -1
\`\`\`

> Pragmatic note: since this repo is brand new, `main` and the branch are
> identical here. The PR will show the full initial set. If a cleaner diff is
> wanted, the executor may instead make `main` = original `docker-compose.yml`
> only, then commit the rest on the branch. Decide at execution time; default is
> the simple single-baseline approach above.

- [ ] **Step 5: Create the public GitHub repo and push.**

Run:
\`\`\`bash
cd ~/Code/text-to-speech
gh repo create text-to-speech --public --source=. --remote=origin --push
\`\`\`
Expected: repo created and `main` pushed. (If `gh repo create` only creates without pushing on this version, follow with `git push -u origin main`.)

- [ ] **Step 6: Push the branch and open the PR.**

\`\`\`bash
cd ~/Code/text-to-speech
git push -u origin feat/no-auth-and-docs
gh pr create --title "Remove required login (anonymous sessions) + add README/.env.example" --body "$(cat <<'EOF'
## Summary
- Enable OpenReader anonymous sessions (`USE_ANONYMOUS_AUTH_SESSIONS=true`) so the reader works without a login. (Auth can't be fully disabled in v4+; this removes the login requirement.)
- Move `AUTH_SECRET`, `BASE_URL`, `ADMIN_EMAILS`, and host ports into a git-ignored `.env`; add committed `.env.example` with placeholders.
- Add `README.md`, `.gitignore`, and `docs/superpowers/` spec + plan.

## Test Plan
- [ ] `cp .env.example .env`, set `AUTH_SECRET`/`ADMIN_EMAILS`, then `docker compose config` resolves with `USE_ANONYMOUS_AUTH_SESSIONS: "true"`.
- [ ] `docker compose up -d` and open http://localhost:3003 — reader loads with no sign-in prompt.
- [ ] No real secret or personal email present in any committed file (only in git-ignored `.env`).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
\`\`\`
Expected: PR URL printed.

---

## Done when

- `docker compose config` shows `USE_ANONYMOUS_AUTH_SESSIONS: "true"` and resolves `AUTH_SECRET`/`BASE_URL`/`ADMIN_EMAILS` from `.env`.
- Public repo `text-to-speech` exists with `README.md`, `.env.example`, `.gitignore`, `docker-compose.yml`, `docs/`.
- `.env` is git-ignored (not in the repo); no committed file contains the real `AUTH_SECRET` or personal email (except the design/plan docs under `docs/`, by deliberate choice).
- PR is open.
